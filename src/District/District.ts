class District {
    id: number;
    numberOfSeats: number;
    name: string;

    constructor(id: number, numberOfSeats: number, name: string) {
        this.id = id;
        this.numberOfSeats = numberOfSeats;
        this.name = name;
    }
}

export default District;
